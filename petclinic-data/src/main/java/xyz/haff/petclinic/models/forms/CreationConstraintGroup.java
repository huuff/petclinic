package xyz.haff.petclinic.models.forms;

/**
 * Used to annotate validation constraints that will only apply when creating a new entity, and not on editing
 */
public interface CreationConstraintGroup {
}
